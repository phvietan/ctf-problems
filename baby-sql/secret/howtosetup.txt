Check .env file, the port number is inside .env file if you want to change port, just change inside .env file and nothing else

PORT_BABY_SQL is the port running the problem
PORTSQL_BABY_SQL is the SQL port

Before running, make sure system has docker and docker-compose

To run:
    cd public
    bash run.sh